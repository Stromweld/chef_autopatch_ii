---
driver:
  name: exec
  host: localhost

provisioner:
  require_chef_omnibus: false
  chef_client_path: 'C:/opscode/chef-workstation/bin/chef-client.bat'
  name: chef_zero
  deprecations_as_errors: false
  install_strategy: skip

platforms:
  - name: windows
